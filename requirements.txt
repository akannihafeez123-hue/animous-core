# HTTP + utils
requests>=2.31.0

# FastAPI / ASGI proxy (if you run the proxy service)
fastapi>=0.95.2
uvicorn[standard]>=0.22.0

# Telegram bot
python-telegram-bot>=20.5

# OpenAI / Anthropic clients (optional, used by llm_utils)
openai>=1.0.0
anthropic>=0.26.0

# Embeddings / transformers
sentence-transformers>=2.2.2
transformers>=4.40.0
torch>=2.1.2  # required backend for many sentence-transformers models; can be heavyweight

# LangChain and optional FAISS vectorstore integration
langchain>=0.1.0
langchain-community>=0.0.29
faiss-cpu>=1.7.4; platform_system != "Windows"

# Misc helpers
python-dotenv>=1.0.0
psutil>=5.9.5

# Development / optional
pytest>=7.4.0
